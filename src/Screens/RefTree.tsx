import React, { useRef, useState, useEffect } from "react";
import { Box, Text } from "@thursday/components";
import { Colors, Grid, Sizes } from "@thursday/utils/const/theme";

interface Props {}

/*
  This component is to play with the idea of a component
  that gets passed via a ref that has getter and setter
  functions that can be accessed in the children.
*/

const RefTree = ({ ...props }: Props): JSX.Element => {
  return <Box></Box>;
};

export { RefTree };
