import React, { useRef, useState, useEffect } from "react";
import { Box, Text } from "@app/Components";
import { Colors, Grid, Sizes } from "@app/theme";

interface Props {}

const DayView = ({ ...props }: Props): JSX.Element => {
  return <Box flex={1}></Box>;
};

export { DayView };
